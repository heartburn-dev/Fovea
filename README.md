# Fovea
Fovea demonstrates that using a small number of images of leaked API keys, we can fine-tune a pre-trained neural network to correctly identify API key leakage, in media, at scale, rather than just in traditional, text-based secret scanning. 
